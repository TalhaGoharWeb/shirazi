{
  "_comment": "Shirazi AI provider registry \u2014 SKELETON for Phase 4 (provider abstraction). Not wired into any code path yet; keys are empty placeholders on purpose. Free-first: providers are ordered by cost (free tiers first). NEVER commit real API keys here \u2014 use config/api_keys.json (git-ignored) or environment variables.",
  "default_provider": "gemini",
  "providers": {
    "gemini": {
      "enabled": true,
      "key_config": "gemini_api_key",
      "key_env": "GEMINI_API_KEY",
      "live_model": "models/gemini-3.1-flash-live-preview",
      "note": "Primary today: Gemini Live voice session + one-shot ladder (core/gemini.py).",
      "text_models": [
        "gemini-2.5-flash",
        "gemini-2.5-flash-lite"
      ]
    },
    "groq": {
      "enabled": false,
      "key_config": "groq_api_key",
      "key_env": "GROQ_API_KEY",
      "models": [],
      "note": "Phase 4: free-tier chat/text fallback."
    },
    "ollama": {
      "enabled": true,
      "model": "llama3.2",
      "note": "Local, keyless. Used today by code_helper/dev_agent via core/llm_client.py.",
      "url": "http://localhost:11434"
    },
    "openrouter": {
      "enabled": false,
      "key_config": "openrouter_api_key",
      "key_env": "OPENROUTER_API_KEY",
      "models": [],
      "note": "Phase 4: free-tier model router fallback."
    }
  }
}